<?xml version="1.0" encoding="UTF-8"?>
<solrs>
   <client id="resume">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/resume</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
    <client id="resumep">
        <!-- 服务器http url  -->
        <url>http://printserver:8083/PlatformSearchServer/resumep</url>
        <!-- 等待读取超时(毫秒)  -->
        <soTimeout>3000</soTimeout>
        <!-- 连接服务器超时(毫秒) -->
        <connectionTimeout>3000</connectionTimeout>
        <!-- 允许一个客户端开启的最大连接数量 -->
        <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
        <!-- 允许开启连接的最大值 -->
        <maxTotalConntion>1000</maxTotalConntion>
        <!-- 是否允许请求转发与重定向 -->
        <followRedirects>false</followRedirects>
        <!-- 是否做传输压缩 -->
        <allowCompression>true</allowCompression>
        <!-- 错误重试次数 -->
        <maxRetries>1</maxRetries>
    </client>
    <client id="news">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/news</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
    <client id="jobinfo">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/jobinfo</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
   
    <client id="hunterjob">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/hunterjob</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
   
    <client id="hunter">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/hunter</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
   
    <client id="company">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/company</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
    <client id="famousperson">
        <!-- 服务器http url  -->
        <url>http://printserver:8083/PlatformSearchServer/famousperson</url>
        <!-- 等待读取超时(毫秒)  -->
        <soTimeout>3000</soTimeout>
        <!-- 连接服务器超时(毫秒) -->
        <connectionTimeout>3000</connectionTimeout>
        <!-- 允许一个客户端开启的最大连接数量 -->
        <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
        <!-- 允许开启连接的最大值 -->
        <maxTotalConntion>1000</maxTotalConntion>
        <!-- 是否允许请求转发与重定向 -->
        <followRedirects>false</followRedirects>
        <!-- 是否做传输压缩 -->
        <allowCompression>true</allowCompression>
        <!-- 错误重试次数 -->
        <maxRetries>1</maxRetries>
    </client>
    <client id="newsphoto">
        <!-- 服务器http url  -->
        <url>http://printserver:8083/PlatformSearchServer/newsphoto</url>
        <!-- 等待读取超时(毫秒)  -->
        <soTimeout>3000</soTimeout>
        <!-- 连接服务器超时(毫秒) -->
        <connectionTimeout>3000</connectionTimeout>
        <!-- 允许一个客户端开启的最大连接数量 -->
        <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
        <!-- 允许开启连接的最大值 -->
        <maxTotalConntion>1000</maxTotalConntion>
        <!-- 是否允许请求转发与重定向 -->
        <followRedirects>false</followRedirects>
        <!-- 是否做传输压缩 -->
        <allowCompression>true</allowCompression>
        <!-- 错误重试次数 -->
        <maxRetries>1</maxRetries>
    </client>
    <client id="newsright">
        <!-- 服务器http url  -->
        <url>http://printserver:8083/PlatformSearchServer/newsright</url>
        <!-- 等待读取超时(毫秒)  -->
        <soTimeout>3000</soTimeout>
        <!-- 连接服务器超时(毫秒) -->
        <connectionTimeout>3000</connectionTimeout>
        <!-- 允许一个客户端开启的最大连接数量 -->
        <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
        <!-- 允许开启连接的最大值 -->
        <maxTotalConntion>1000</maxTotalConntion>
        <!-- 是否允许请求转发与重定向 -->
        <followRedirects>false</followRedirects>
        <!-- 是否做传输压缩 -->
        <allowCompression>true</allowCompression>
        <!-- 错误重试次数 -->
        <maxRetries>1</maxRetries>
    </client>
    <client id="resumeinfo">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/resumeinfo</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
    <client id="sparecorpinfo">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/sparecorpinfo</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
    <client id="hrmanage">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/hrmanage</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
    <client id="jobinfos">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/jobinfos</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
    <client id="resumework">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/resumework</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
    <client id="resumeedu">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/resumeedu</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
    <client id="jobinfos">
      <!-- 服务器http url  -->
      <url>http://printserver:8083/PlatformSearchServer/jobinfos</url>
      <!-- 等待读取超时(毫秒)  -->
      <soTimeout>3000</soTimeout>
      <!-- 连接服务器超时(毫秒) -->
      <connectionTimeout>3000</connectionTimeout>
      <!-- 允许一个客户端开启的最大连接数量 -->
      <defaultMaxConnectionsPerHost>1000</defaultMaxConnectionsPerHost>
      <!-- 允许开启连接的最大值 -->
      <maxTotalConntion>1000</maxTotalConntion>
      <!-- 是否允许请求转发与重定向 -->
      <followRedirects>false</followRedirects>
      <!-- 是否做传输压缩 -->
      <allowCompression>true</allowCompression>
      <!-- 错误重试次数 -->
      <maxRetries>1</maxRetries>
   </client>
</solrs>